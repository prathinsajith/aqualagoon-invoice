# Not a real pnpm workspace — this file only approves the native
# post-install build scripts pnpm blocks by default (pnpm 11 supply-chain guard).
#   - sharp:         image optimization used by next/image
#   - unrs-resolver: native resolver used by eslint / eslint-config-next
allowBuilds:
  sharp: true
  unrs-resolver: true
